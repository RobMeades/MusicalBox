# Introduction
Stepper is a generic stepper motor driver for ESP32.  It is intended to run an on ESP32 which is slaved to a known server over Wifi.  The server is responsible for updating the FW version running on the ESP32, orchestrating the behaviour of multiple ESP32s and monitoring their state.
